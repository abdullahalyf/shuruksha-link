# Shuruksha Link - Development environment
# Vite reads .env.development automatically when running `npm run dev`.
# Leave VITE_API_BASE_URL empty so the dev-server proxy in
# vite.config.js routes /api -> http://localhost:5000.
VITE_API_BASE_URL=
VITE_APP_ENV=development
